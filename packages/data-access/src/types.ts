export type Composition = {
   assets: string[];
   allocations: number[];
};

export type token = {
   symbol: string;
   name: string;
   address: string;
   decimals: number;
   chainId: number;
   logoURI: string;
};
